#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main() {
    
    FILE* fp = fopen("input2.txt", "r");
    int buff;

    while(fscanf(fp, "%d", &buff) != EOF){
        printf("%d\n", buff);
    }

    return 0;
}
